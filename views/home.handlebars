<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        h1 { color: #333; }
        ul { list-style-type: none; padding: 0; }
        li { margin: 10px 0; }
        a { text-decoration: none; color: #0066cc; }
        a:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <h1>{{title}}</h1>
    <h2>Photo Albums</h2>
    
    {{#if albums.length}}
        <ul>
            {{#each albums}}
                <li>• <a href="/album/{{this.name}}">{{this.name}}</a></li>
            {{/each}}
        </ul>
    {{else}}
        <p>No albums found.</p>
    {{/if}}
</body>
</html>